<Window x:Class="OneDrive_Cloud_Player.VLC.VideoPlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OneDrive_Cloud_Player.VLC" 
        xmlns:wpf="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="VideoPlayerWindow" Height="450" Width="688">
    <Window.Resources>
        <local:VideoPlayerViewModel x:Key="VideoPlayerVM"/>
    </Window.Resources>
    <Grid>
        <wpf:VideoView x:Name="videoView" Panel.ZIndex="1">
            <StackPanel Orientation="Horizontal" x:Name="test">
                <Button Content="{Binding Source={StaticResource VideoPlayerVM}, Path=PausePlayButtonTitle}" Height="25" Width="50" VerticalAlignment="Bottom" HorizontalAlignment="Left" Click="PauseContinueButton_Click" />
                <Button Content="STOP" Height="25" Width="50" VerticalAlignment="Bottom" HorizontalAlignment="Left" Click="StopButton_Click" />
                <Button Content="NEW" Height="25" Width="50" VerticalAlignment="Bottom" HorizontalAlignment="Left" Click="NewVideoButton_Click" />
            </StackPanel>
        </wpf:VideoView>
    </Grid>

    <!--<StackPanel>
            <Button Content="Play" Width="60" Height="20" Command="{Binding PlayVideoCommand, Source={StaticResource VideoPlayerVM}}" />
            <Button Content="Stop" Width="60" Height="20" Command="{Binding StopVideoCommand, Source={StaticResource VideoPlayerVM}}"/>
            <Button Content="Pause" Width="60" Height="20" Command="{Binding PauseVideoCommand, Source={StaticResource VideoPlayerVM}}"/>
        </StackPanel>


        <local:VideoPlayerControls Grid.Column="2" HorizontalAlignment="Left" Height="0" Margin="517,150,0,0" VerticalAlignment="Top" Width="12"/>-->

</Window>
